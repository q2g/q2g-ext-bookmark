{
    // The following will hide the js and map files in the editor
    "files.exclude": {       
        "**/*.js": {
            "when": "$(basename).ts"
        },
         "src/lib/daVinci.js":false,
        "**/*.map": true,
        "**/*.css": true
    }
}